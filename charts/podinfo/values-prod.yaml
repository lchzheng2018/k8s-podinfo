replicaCount: 3

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "1000m"
    memory: "512Mi"

hpa:
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 60